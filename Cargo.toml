[package]
name = "seeds"
version = "0.1.0"
edition = "2021"

[dependencies]
cust = "0.3.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[build-dependencies]
cc = "1.0"

[profile.release]
opt-level = 3
